[gcode_macro _idexLeftProbeOffset]
gcode = 
	SET_GCODE_VARIABLE MACRO=DC_VARS VARIABLE=left_probe VALUE={printer.probe.last_z_result|round(3, 'common')}

    RESPOND TYPE=echo MSG= "_idexLeftProbeOffset inside"
    M400 # Wait for all movements to finish