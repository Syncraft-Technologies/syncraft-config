[gcode_macro _idexRightProbeOffset]
gcode = 
	SET_GCODE_VARIABLE MACRO=DC_VARS VARIABLE=right_probe VALUE={printer.probe.last_z_result|round(3, 'common')}

    RESPOND TYPE=echo MSG="_idexLeftProbeOffset inside"
    M400 # Wait for all movements to finish