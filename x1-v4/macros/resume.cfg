
[gcode_macro RESUME]
description: Resume seguro (restaura estado)
rename_existing: RESUME_BASE
gcode:
  {% if printer["gcode_macro _setLedPrinting"] is defined %}
    _setLedPrinting
  {% endif %}
  RESTORE_GCODE_STATE NAME=PAUSE_state MOVE=1
  RESUME_BASE
